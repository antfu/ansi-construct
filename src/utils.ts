export function space(length = 1): string {
  return ' '.repeat(length)
}

export function newline(length = 1): string {
  return '\n'.repeat(length)
}

export { default as colors } from 'picocolors'
